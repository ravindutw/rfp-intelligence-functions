# Direct dependencies only — transitive deps are resolved automatically by pip

# AWS SDK
boto3>=1.42.51

# LangChain ecosystem
langchain-community>=0.4.1
langchain-text-splitters>=1.1.0

# PDF parsing (peer dependency of PyPDFLoader)
pypdf>=6.7.0

# GCP authentication & Gemini SDK
google-auth>=2.48.0
google-genai>=1.14.0

# Vector database
pymilvus>=2.5.8
